# B-private live credential template (versioned; EMPTY values only).
#
# Production path on VPS (outside git):
#   /etc/spread/bbot-private-live.env
#   mode 0600, owner root:root
#
# Futures-only (USDT perpetual). Spot keys must not be used here.
#
# Do NOT put VENUE or LIVE_ORDERS in this credential file.
# Runtime venue/flags are process env, not credentials.
# Live order send requires a separate runtime approval later — credentials
# alone never authorize send.
#
# Copy to a non-git path, fill values only on disk. Never commit real values.

BYBIT_LIVE_API_KEY=
BYBIT_LIVE_API_SECRET=
OKX_LIVE_API_KEY=
OKX_LIVE_API_SECRET=
OKX_LIVE_PASSPHRASE=
