# Гир 2 — снимок закрытия этапов 2–4 (2026-08-25)

Трек **M**. Не D, не B. Не объявление торгового преимущества.

**Статус гира 2:** гир 2 = закрыт (контур; 2.2 вне scope). Без скобок фраза запрещена.

Доказательство = **фильтры и число сигналов** на 4h `2026-08-18 00:00–04:00 UTC`, `closed=0`, **не PnL**. Канон счётчиков — **эта таблица**, не executed cells [`model_gear2.ipynb`](../model_gear2.ipynb).

Integration Validator (2026-08-25): **YELLOW** — независимый 4h A/B MATCHED эту таблицу 1:1; pytest 28 passed; гир 1.0 при флагах выкл. держится. Review Critic: **Accept with caveats** (штамп только со скобками; C/D → 2.2; ноутбук не канон).

C (`regime_on`) и D (случайный вход на частоте B) **не входят** — гир 2.2. `K>1` не требуется и не делался. Alpha не утверждается.

**2026-08-27:** GD зафиксировал гир 2 как закрытый контур; **следующий разрешённый этап M = гир 2.2** (stats/C/D). Гиры 2.5 и 3 не стартуют из этого снимка.

---

## Окно прогона A/B

| | |
|--|--|
| Среда | local Mac, `output/lean_ticks` |
| UTC | `2026-08-18T00:00:00Z` → `2026-08-18T04:00:00Z` (`END` не входит) |
| Вселенная | `is_crypto`, 240 монет, 10 731 288 тиков после fail-closed |
| `K` | 1 |
| `VARIATION` | пороги 0.5, `open_frac`/`close_frac` 0.7 (**не** поиск) |
| `HYPER` 1.0 | `Trade_Lat=100`, `fee_rate=0.00075`, `avg_window_sec=10`, `Check_volume=False` |
| `max_latency_*` | pooled p95 этого среза: OKX 54.0 мс, Bybit 35.0 мс (правило 1.0, не ретьюнинг) |
| Arm A | `regime_topn=None` |
| Arm B | Top‑10, open only, score бара `[t−5m, t)` для тиков `≥ t`, `REGIME_TOP_N=10` |

Ноутбук [`model_gear2.ipynb`](../model_gear2.ipynb) держит узкий debug `CONFIG` (`END` 02:00, `MAX_COINS=7`); saved outputs зачищены 2026-08-25, чтобы файл нельзя было читать как воспроизведение этой таблицы. Канон счётчиков — здесь.

---

## Таблица отсевов (не PnL)

Сравнение веток — **фильтры и число сигналов**. Победителя по прибыли нет: `closed=0` у обеих, `metric` в таблицу не входит.

| arm | closed | open_at_end | raw | passed | slot_busy | pending_skip | freshness | latency | avg | size | not_topn | pending_missed |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A флаг выкл. | 0 | 1 | 12 | 1 | 1007 | 0 | 0 | 0 | 11 | 0 | 0 | 0 |
| B Top‑N open | 0 | 1 | 228 | 1 | 1007 | 0 | 0 | 0 | 0 | 0 | 227 | 0 |

Наблюдения (механика, не alpha):

- A: 11 из 12 raw не прошли Gate B (`avg`); 1 open дошёл до fill и остался `open_at_end`.
- B: 227 raw отсечены `not_topn` на **open**; close-гейта нет. 1 passed, тоже `open_at_end`.
- `n_filtered_pending_skip=0` на этом окне: `Trade_Lat=100` мс, чужая монета с порогом 0.5 за окно pending не попалась. Счётчик **есть**; инкремент доказан синтетикой в `tests/test_gear2_invariants.py`. На живой 4h ленте **не наблюдался**.
- `slot_busy` совпал (1007). Это не критерий выбора ветки и **не** доказательство одинаковой занятости: `raw` 12 vs 228 при `pending_skip=0` допускает разный момент fill. Печать `open_position.base_coin` / `open_ts` по A vs B на этом 4h срезе **не делалась** (caveat Critic; дорогой повтор 10M тиков в этом штампе пропущен).
- Top-N = **open-only флаг**. Удержание монеты ≠ членство в кластере 1.5.

Инварианты `K=1` (нет двух пересекающихся позиций, ≤1 `open_at_end`) прошли на этом прогоне и в pytest.

---

## Эталон v0 (arm A)

При `USE_REGIME_TOPN=False` / `regime_topn=None`: `not_topn=0`, elif 1.0 не менялся, новый счётчик — только `pending_skip`. Сделки / `slot_busy` / гейты A/B — прежняя семантика занятости **после fill**; skips во время `pending` больше не молчаливые.

---

## Дыры этого снимка

- Четыре часа 18.08, не полный календарь покрытия ([`model-data-coverage.md`](model-data-coverage.md)).
- В этом срезе `no_hist` по тиковым монетам пуст; **QNT/USDC** по-прежнему без hist-баров на полном календаре (fail-closed на open, если попадут в срез).
- HTML обзор монет (`output/gear2_coin_overview/`) не в git.
- `K>1` не делался (не требуется для этого close).
- `closed=0`: round-trip close на приёмочной ленте не видели; close-path — синтетика pytest.
- Occupancy identity A vs B (`base_coin` / `open_ts`) на 4h не печаталась.
- Контракт порядка гейтов = код [`research/gear2_backtest.py`](../research/gear2_backtest.py): Top-N на open **до** Gate A. Схема mermaid/dot приведена к коду; при расхождении верить коду.
