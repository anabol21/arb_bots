# Runtime + dev dependencies for the spread pipeline (Cloud Agent environment).
# The repo-root requirements.txt is a hand-maintained import inventory that also
# lists stdlib modules, so it is not pip-installable. This file pins only the
# real third-party packages the code imports, validated on Python 3.12.
pandas==3.0.5
pyarrow==25.0.1
duckdb==1.5.5
plotly==7.0.0
websockets==17.1
websocket-client==1.9.2
uvloop==0.22.1
pytest==9.1.1
numpy>=2,<3
ccxt==4.5.76
